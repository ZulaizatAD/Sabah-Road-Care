# POST   /potholes/              # Create new pothole report
# GET    /potholes/              # Get all reports (with filters)
# GET    /potholes/my-reports    # Get current user's reports
# GET    /potholes/{report_id}   # Get specific report
# PUT    /potholes/{report_id}   # Update report (admin)
# DELETE /potholes/{report_id}   # Delete report
# POST   /potholes/{report_id}/files  # Upload files to report